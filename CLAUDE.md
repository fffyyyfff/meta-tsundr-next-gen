@AGENTS.md
<!-- TMUX-AGENT-CONFIG -->
## tmux マルチエージェント環境

このプロジェクトはtmux上でマルチエージェント並列実行しています。

### ペイン構成 (Layout: triple)
- **Orchestrator**: `%0` — タスク分解・割り当て・進捗管理
- **Worker1**: `%1` — タスク実行
- **Worker2**: `%2` — タスク実行

### ペイン間通信コマンド

**部下ペインにタスクを送信:**
```bash
./.tmux-send-task.sh %1 "タスクの内容"
```

**部下ペインの出力をキャプチャ（最新30行）:**
```bash
./.tmux-capture.sh %1 30
```

**全ワーカーに一斉送信:**
```bash
./.tmux-broadcast.sh "全員への指示"
```

**全ペインの状態確認:**
```bash
./.tmux-status.sh
```

### オーケストレーターのルール
1. タスクを分解して各ワーカーに`./.tmux-send-task.sh`で送信
2. 定期的に`./.tmux-capture.sh`で進捗を確認
3. 結果を回収・統合して次のタスクを割り当て
4. トークン消費が多い場合は`/clear`を指示
5. 報告形式: `[pane_id] 完了/進捗/エラー: 内容`
<!-- END-TMUX-AGENT-CONFIG -->

## 証跡（Evidence）ルール

タスク完了時は **毎回** 以下の証跡を自動で作成すること。指示がなくても必ず実施する。

### 手順
1. `evidence/night-run/YYYYMMDD-HHMMSS/` ディレクトリを作成
2. 検証を実施しログを保存:
   - `typecheck.log` — `npx tsc --noEmit` の出力
   - `build.log` — `npx next build` の出力
3. Playwright でスクリーンショットをキャプチャ:
   - アプリを起動し、変更した画面を撮影（最低1枚、関連画面すべて）
   - `01-xxx.png`, `02-xxx.png` の連番で保存
4. `summary.md` を作成（以下のフォーマット）:
   - セッション概要（日時、構成、タスク数）
   - 完了タスク一覧（テーブル: #, Pane, タスク, 主な対象ファイル）
   - 変更ファイル一覧（新規/更新をテーブルで）
   - 検証結果（テーブル: 検証項目, 結果, ログファイル）
   - スクリーンショット（`![説明](./ファイル名)` 形式で埋め込み + 説明文）
   - プロジェクト統計（ファイル数、行数、エラー数等）

### 注意事項
- 証跡作成は **オーケストレーターへの報告前** に完了させること
- TypeCheck と Build が PASS であることを確認してから報告
- スクリーンショットは実際の画面を撮影し、変更が反映されていることを目視確認
